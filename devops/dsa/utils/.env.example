# Copy to .env and set your values. The .env file is gitignored.
#
#   cp .env.example .env
#
# Optional: pip install python-dotenv (annotation_points.py loads .env automatically
# if that package is installed; otherwise export these variables in your shell.)

GIRDER_API_URL=http://localhost:8080/api/v1
GIRDER_API_KEY=
